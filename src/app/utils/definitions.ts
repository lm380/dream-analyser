export type User = {
  userid: string;
  name: string;
  email: string;
  password: string;
};

export type Dream = {
  dreamid: string;
  content: string;
  keyelements: string;
  userid: string;
};
